// ─── Mission Academy — Section Unlock Config ──────────────────────────────
// This file is managed by the Teacher Panel (type "dev" on any page).
// All sections are open by default so teachers do not need mission unlock codes.
// Use the dev panel only if you intentionally want to hide later sections.

window.SITE_CONFIG = {
  math_open_sections:     6,
  ela_open_sections:      12,
  reading_open_sections:  4,
  spelling_open_sections: 4,
  updated: "2026-06-24"
};
